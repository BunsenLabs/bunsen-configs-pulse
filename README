bunsen-configs-pulse
====================

This package holds configuration files for Bunsen Labs installations
which use PulseAudio.

If PulseAudio is removed from the system,
this package should be removed too.

Current contents are:
/etc/asound.conf
/etc/pulse/daemon.conf.d/bunsen.conf
